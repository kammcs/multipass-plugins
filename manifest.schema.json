{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://multipass.theater/schema/plugin-manifest-1.json",
  "title": "Multipass plugin manifest",
  "description": "manifest.json at the root of a .mpp package. This is the document that gets signed, and it names every other file with its hash. Point your editor at this schema for validation and completion as you type. The server's own validator is the authority; a Go contract test keeps the two in step.",
  "type": "object",
  "required": [
    "id",
    "name",
    "version",
    "apiVersion"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Reverse-DNS, permanent: it is the identity everything else hangs off.",
      "pattern": "^[a-z0-9][a-z0-9.-]*[a-z0-9]$",
      "minLength": 3,
      "maxLength": 128,
      "examples": [
        "com.example.myplugin"
      ]
    },
    "name": {
      "type": "string",
      "description": "What an owner sees.",
      "minLength": 1,
      "maxLength": 80
    },
    "version": {
      "type": "string",
      "description": "Your version string. The registry refuses a duplicate.",
      "minLength": 1,
      "maxLength": 32
    },
    "apiVersion": {
      "type": "integer",
      "description": "The plugin API you were built against. A server refuses a package whose apiVersion it does not speak, so an old plugin never half-works.",
      "enum": [
        1
      ]
    },
    "minHostVersion": {
      "type": "string",
      "description": "The oldest Multipass release that can draw everything you send, as major.minor.patch. Name one when you use a block type, slot or op that arrived in a particular release: apiVersion says the contract is the same, this says the build is new enough. An older server refuses the install and says which version it needs, which is a better failure than running and drawing nothing. Leave it out if you use nothing new.",
      "pattern": "^[0-9]{1,6}\\.[0-9]{1,6}\\.[0-9]{1,6}$"
    },
    "entry": {
      "type": "string",
      "description": "The .wasm file inside the package. Required for every family except a theme pack, which is data and has no code to point at.",
      "pattern": "^[A-Za-z0-9._-]+\\.wasm$"
    },
    "description": {
      "type": "string",
      "description": "One or two sentences, shown in the registry and in the owner's plugin list.",
      "maxLength": 500
    },
    "author": {
      "type": "string",
      "maxLength": 120
    },
    "homepage": {
      "type": "string",
      "description": "Where to read more.",
      "format": "uri",
      "pattern": "^https://"
    },
    "provides": {
      "type": "array",
      "description": "The families you implement.",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "matching",
          "fields",
          "search",
          "events",
          "subtitles",
          "segments",
          "theme",
          "pages",
          "source",
        "channel"
        ]
      }
    },
    "events": {
      "type": "array",
      "description": "The events you ask to be told about. Declared rather than requested at runtime, so the install screen can say in plain words what you will learn. Requires the events family, and an events plugin must name at least one.",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "playback.started",
          "playback.paused",
          "playback.resumed",
          "playback.stopped",
          "playback.finished"
        ]
      }
    },
    "capabilities": {
      "type": "object",
      "description": "The powers you ask the owner to grant. Absent means none at all, which the install screen says out loud. A CLOSED set: an unknown key fails the parse, because a grant the host cannot enforce is one the owner would be shown and we would not apply.",
      "additionalProperties": false,
      "properties": {
        "http": {
          "type": "array",
          "description": "Outbound HTTPS hosts. One leading '*.' wildcard label is allowed. Ports and schemes are not part of a grant: the host function only ever speaks https on the default port, and private, loopback and link-local addresses are refused after DNS with every redirect re-checked.",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^(\\*\\.)?[a-z0-9]([a-z0-9-]*[a-z0-9])?(\\.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+$"
          }
        },
        "storage": {
          "type": "boolean",
          "description": "A key-value bucket isolated by plugin id. Removing the plugin removes the bucket."
        },
        "library": {
          "type": "boolean",
          "description": "The narrow library lookup: external metadata ids in, local item ids out. There is no search, no listing and no filter behind it, so you can ask whether titles you already know about are here and cannot find out what else is."
        }
      }
    },
    "pages": {
      "type": "array",
      "description": "The pages you serve. A page must be declared to be addressable: the server answers an undeclared id with a 404 and never starts your plugin. Requires the pages family, and a pages plugin must declare at least one.",
      "maxItems": 8,
      "items": {
        "type": "object",
        "required": [
          "id",
          "title"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "description": "Lowercase letters, digits and dashes. It is part of the page's address on every surface.",
            "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
            "maxLength": 40
          },
          "title": {
            "type": "string",
            "description": "What the page is called when a render does not override it.",
            "minLength": 1,
            "maxLength": 80
          },
          "subject": {
            "type": "string",
            "description": "What this page's reserved 'id' param names. The host resolves it before calling you, which is why a page about a person needs no permissions at all.",
            "enum": [
              "item",
              "person"
            ]
          },
          "cacheSeconds": {
            "type": "integer",
            "description": "How long a render may be reused. Absent or zero means 900. The cards inside a container block are resolved fresh for every viewer regardless.",
            "minimum": 0,
            "maximum": 86400
          }
        }
      }
    },
    "settings": {
      "type": "array",
      "description": "Settings a person edits, rendered as rows in your plugin's card. Scope decides whose they are. For an events plugin the server-scoped ones are the SERVER-wide values (an application's credentials, say); each person's own account is connected separately and never appears here.",
      "maxItems": 32,
      "items": {
        "type": "object",
        "required": [
          "key",
          "label",
          "type"
        ],
        "additionalProperties": false,
        "properties": {
          "key": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64
          },
          "label": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          },
          "type": {
            "type": "string",
            "description": "A password setting is write-only to the API: an owner can replace it, never read it back out of the server they are configuring.",
            "enum": [
              "text",
              "password",
              "number",
              "toggle",
              "select"
            ]
          },
          "default": {
            "type": "string"
          },
          "options": {
            "type": "array",
            "description": "Required when type is select.",
            "items": {
              "type": "string"
            }
          },
          "help": {
            "type": "string",
            "maxLength": 500
          },
          "scope": {
            "type": "string",
            "description": "Whose setting this is. Absent means server: the owner sets one value for the household. profile means each person sets their own on their own settings screen, with your default as the fallback; a password cannot be per-profile, because a value its owner can read back is not a secret.",
            "enum": [
              "server",
              "profile"
            ]
          }
        }
      }
    },
    "actions": {
      "type": "array",
      "description": "Buttons the host draws beside your settings. Pressing one calls the settings.action op with that id and shows the sentence you return. An action may not return blocks.",
      "maxItems": 8,
      "items": {
        "type": "object",
        "required": [
          "id",
          "label"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9-]+$",
            "minLength": 1,
            "maxLength": 40
          },
          "label": {
            "type": "string",
            "minLength": 1,
            "maxLength": 60
          },
          "scope": {
            "type": "string",
            "description": "server draws the button in the owner's plugin card and only the owner may press it; profile draws it on each person's own settings screen. Absent means server.",
            "enum": [
              "server",
              "profile"
            ]
          },
          "help": {
            "type": "string",
            "maxLength": 500
          }
        }
      }
    },
    "themes": {
      "type": "array",
      "description": "What a theme pack ships. Requires the theme family, and a theme plugin must declare at least one. There is no code in this family: the signed manifest is the whole payload, so every field is validated at install.",
      "maxItems": 12,
      "items": {
        "type": "object",
        "required": [
          "id",
          "name",
          "accent",
          "focus",
          "bg",
          "gradient",
          "panel",
          "text",
          "dim"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "description": "Lowercase letters, digits and dashes. Namespaced by your plugin id before anyone sees it.",
            "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
            "minLength": 2,
            "maxLength": 40
          },
          "name": {
            "type": "string",
            "description": "What the theme is called in the picker.",
            "minLength": 1,
            "maxLength": 40
          },
          "accent": {
            "type": "string",
            "pattern": "^#[0-9a-fA-F]{6}$",
            "description": "Recolours every accented control."
          },
          "focus": {
            "type": "string",
            "pattern": "^#[0-9a-fA-F]{6}$",
            "description": "The focus ring and selection highlight."
          },
          "bg": {
            "type": "string",
            "pattern": "^#[0-9a-fA-F]{6}$",
            "description": "The page background base."
          },
          "gradient": {
            "type": "array",
            "description": "Exactly two stops for the page gradient.",
            "minItems": 2,
            "maxItems": 2,
            "items": {
              "type": "string",
              "pattern": "^#[0-9a-fA-F]{6}$"
            }
          },
          "panel": {
            "type": "string",
            "pattern": "^#[0-9a-fA-F]{6}$",
            "description": "Card and panel surfaces."
          },
          "text": {
            "type": "string",
            "pattern": "^#[0-9a-fA-F]{6}$",
            "description": "Body text."
          },
          "dim": {
            "type": "string",
            "pattern": "^#[0-9a-fA-F]{6}$",
            "description": "Secondary text."
          },
          "cardRadius": {
            "type": "integer",
            "description": "Corner rounding, px on web and dp on native.",
            "minimum": 0,
            "maximum": 32
          },
          "bgImage": {
            "type": "string",
            "description": "A .jpg or .png inside your own package, shown behind the app on TV and native. Hash-verified like every packaged file.",
            "pattern": "^[A-Za-z0-9._-]+\\.(jpg|png)$"
          }
        }
      }
    },
    "channel": {
      "type": "object",
      "description": "A channel (D177): several libraries behind one card, with a page of your own. Requires the channel family AND the source family, since its libraries sync and play through the source ops, and a manifest declares source or channel, never both.",
      "required": [
        "name",
        "libraries",
        "layout"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "The channel, as a person reads it on the card and at the top of its page.",
          "minLength": 1,
          "maxLength": 80
        },
        "label": {
          "type": "string",
          "description": "The line under the name.",
          "maxLength": 120
        },
        "art": {
          "type": "string",
          "description": "The card's picture: a plain .jpg or .png file name inside your own package, so it is hash-verified like everything else you ship. A 16:9 picture is the shape to draw.",
          "pattern": "^[^/\\\\]+\\.(jpg|png)$"
        },
        "account": {
          "type": "string",
          "description": "Whether this channel belongs to a person. It is the channel's answer for all of its libraries: a per-profile channel is created whole when somebody presses Connect and removed whole when they Disconnect, and its card is on the shelf only for them. profile requires the events family, which is what Connect runs.",
          "enum": [
            "none",
            "profile"
          ]
        },
        "libraries": {
          "type": "array",
          "description": "The libraries this channel owns. Each is an ordinary source declaration plus a key and a name; the key rides on every sync and resolve so you know which library you are being asked about, and a library entry cannot declare an account of its own.",
          "minItems": 1,
          "maxItems": 6,
          "items": {
            "type": "object",
            "required": [
              "key",
              "name",
              "shape"
            ],
            "additionalProperties": false,
            "properties": {
              "key": {
                "type": "string",
                "description": "Lowercase letters, digits and dashes, unique within the plugin. Keep it stable across versions: changing it is a new library.",
                "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
                "maxLength": 40
              },
              "name": {
                "type": "string",
                "description": "What this library is called.",
                "minLength": 1,
                "maxLength": 80
              },
              "shape": {
                "type": "string",
                "description": "The library's type.",
                "enum": [
                  "movie",
                  "show"
                ]
              },
              "refreshMinutes": {
                "type": "integer",
                "description": "How often the host re-syncs this library. Absent or zero means 360. Anything under 15 is refused rather than clamped.",
                "minimum": 15
              },
              "settings": {
                "type": "array",
                "description": "The fields the owner fills in when the library is added. A per-profile channel's libraries cannot declare them: nothing would ever fill them in.",
                "maxItems": 32,
                "items": {
                  "type": "object",
                  "required": [
                    "key",
                    "label",
                    "type"
                  ],
                  "additionalProperties": false,
                  "properties": {
                    "key": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 64
                    },
                    "label": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 120
                    },
                    "type": {
                      "type": "string",
                      "enum": [
                        "text",
                        "password",
                        "number",
                        "toggle",
                        "select"
                      ]
                    },
                    "default": {
                      "type": "string"
                    },
                    "options": {
                      "type": "array",
                      "description": "Required when type is select.",
                      "items": {
                        "type": "string"
                      }
                    },
                    "help": {
                      "type": "string",
                      "maxLength": 500
                    }
                  }
                }
              }
            }
          }
        },
        "layout": {
          "type": "array",
          "description": "The page you ship. The household then owns it: the stored document holds their edits and this stays the default underneath. Ids are the merge key, so keep them stable.",
          "minItems": 1,
          "maxItems": 16,
          "items": {
            "type": "object",
            "required": [
              "id",
              "kind"
            ],
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string",
                "description": "Lowercase letters, digits and dashes. It is the merge key between your default and the household's edits.",
                "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
                "maxLength": 40
              },
              "kind": {
                "type": "string",
                "description": "What this row is. Everything but slot is drawn by the host out of what it already has; slot is yours, and is the one kind an owner can hide but never add.",
                "enum": [
                  "hero",
                  "libraryShelf",
                  "collectionShelf",
                  "heading",
                  "slot"
                ]
              },
              "library": {
                "type": "string",
                "description": "Which of this channel's libraries feeds the row. Required for hero and libraryShelf, and a key this channel does not own is refused.",
                "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
                "maxLength": 40
              },
              "collection": {
                "type": "integer",
                "description": "A collection in this household, for a collectionShelf. Owner-added only: you cannot know a household's collection ids.",
                "minimum": 1
              },
              "sort": {
                "type": "string",
                "description": "The row's ordering. These are the library view's own sorts, so a row of yours costs no more than one of ours. unwatched is newest first with anything the viewer has finished left out.",
                "enum": [
                  "recent",
                  "title",
                  "rating",
                  "release",
                  "unwatched"
                ]
              },
              "shape": {
                "type": "string",
                "description": "Which card a shelf draws. A channel row holds titles, so it is one of these two rather than the tile vocabulary's four.",
                "enum": [
                  "wide",
                  "poster"
                ]
              },
              "limit": {
                "type": "integer",
                "description": "How many titles the row holds. Absent or zero means the default: 20 for a shelf, 5 for a hero. A hero holds 1 to 10, a shelf 4 to 24.",
                "minimum": 0,
                "maximum": 24
              },
              "title": {
                "type": "string",
                "description": "The row's heading.",
                "maxLength": 120
              },
              "text": {
                "type": "string",
                "description": "A heading block's line.",
                "maxLength": 120
              },
              "rotateSeconds": {
                "type": "integer",
                "description": "How fast a hero pages through its titles. 0 is off.",
                "minimum": 0,
                "maximum": 60
              },
              "cacheSeconds": {
                "type": "integer",
                "description": "How long a slot's render may be reused. Absent or zero means 900. The cards inside it are resolved fresh for every viewer regardless.",
                "minimum": 0,
                "maximum": 86400
              }
            }
          }
        }
      }
    },
    "source": {
      "type": "object",
      "description": "What kind of library you are (D176). Requires the source family, and a source plugin must declare one.",
      "required": [
        "shape"
      ],
      "additionalProperties": false,
      "properties": {
        "shape": {
          "type": "string",
          "description": "The library's type. A show-shaped library is a channel per series with its uploads as episodes.",
          "enum": [
            "movie",
            "show"
          ]
        },
        "account": {
          "type": "string",
          "description": "Whether a library of this kind belongs to a person. none: the owner adds it like a folder and the household shares it. profile: it is created when somebody presses Connect and removed when they Disconnect, it is private to them, and its syncs run as them. profile requires the events family, which is what Connect runs.",
          "enum": [
            "none",
            "profile"
          ]
        },
        "refreshMinutes": {
          "type": "integer",
          "description": "How often the host re-syncs. Absent or zero means 360. Anything under 15 is refused rather than clamped.",
          "minimum": 15
        },
        "settings": {
          "type": "array",
          "description": "The fields the owner fills in when adding a library of this kind (a feed URL, a region). They belong to the LIBRARY, so two libraries from one plugin have their own, and none of them can be per-profile.",
          "maxItems": 32,
          "items": {
            "type": "object",
            "required": [
              "key",
              "label",
              "type"
            ],
            "additionalProperties": false,
            "properties": {
              "key": {
                "type": "string",
                "minLength": 1,
                "maxLength": 64
              },
              "label": {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              "type": {
                "type": "string",
                "description": "A password setting is write-only to the API: an owner can replace it, never read it back out of the server they are configuring.",
                "enum": [
                  "text",
                  "password",
                  "number",
                  "toggle",
                  "select"
                ]
              },
              "default": {
                "type": "string"
              },
              "options": {
                "type": "array",
                "description": "Required when type is select.",
                "items": {
                  "type": "string"
                }
              },
              "help": {
                "type": "string",
                "maxLength": 500
              }
            }
          }
        }
      }
    },
    "limits": {
      "type": "object",
      "description": "Your declared budgets. Zero or absent means the default, never unlimited: there is no way to express unlimited.",
      "additionalProperties": false,
      "properties": {
        "memoryPages": {
          "type": "integer",
          "description": "64 KiB pages of linear memory. Default 256 (16 MiB).",
          "minimum": 1,
          "maximum": 1024
        },
        "timeoutMs": {
          "type": "integer",
          "description": "How long one call may run. Default 5000. A call past its deadline is killed by closing the module; the next call rebuilds it, so a timeout costs latency, not a dead plugin.",
          "minimum": 1,
          "maximum": 30000
        }
      }
    },
    "files": {
      "type": "array",
      "description": "Every packaged file with its sha256. You do not write this: 'multipass plugins pack' hashes everything and fills it in.",
      "items": {
        "type": "object",
        "required": [
          "name",
          "sha256"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "sha256": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$"
          }
        }
      }
    }
  }
}
